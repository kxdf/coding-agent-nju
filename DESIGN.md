# Coding Agent 设计说明

这份文档用于解释项目原理和设计取舍，不属于最终 zip 中的 `README.txt`。

## 1. 项目目标与边界

项目实现一个最小但完整的编程智能体：大语言模型负责判断下一步动作，本地 Python 程序负责维护对话、执行文件或命令工具，并决定何时停止。

项目直接调用 OpenAI 兼容的 Chat Completions 与原生 tool calling 接口，没有使用 LangChain、AutoGen、OpenAI Agents SDK 等 Agent 框架，也没有使用 API 服务端托管的代码执行或文件工具。

## 2. 核心运行流程

```text
用户任务
  -> system/user 消息
  -> 请求模型（携带本地工具 schema）
  -> 模型返回普通文本或 tool_calls
       -> 普通文本：作为最终答案结束
       -> tool_calls：本地检查并执行工具
  -> 工具结果作为 role=tool 消息加入历史
  -> 再次请求模型
  -> finish_task 或达到最大步数后结束
```

关键点是：模型不能直接访问本机。模型只生成结构化的工具调用请求，是否执行以及执行结果都由本地 `ToolBox` 控制。

可选三 Agent 模式在这个单 Agent 循环外增加顺序协作：

```text
用户任务
  -> Planner：只读检查并调用 submit_plan 提交结构化计划
  -> Executor：按照计划修改文件、运行测试并调用 finish_task
  -> Reviewer：独立读文件、重新运行测试并调用 finish_review
       -> approved=true：完成
       -> approved=false：Executor 根据 issues 返工一次，再次审查
```

三个 Agent 是使用同一个模型的三个逻辑角色，不是三个进程。它们拥有不同的 system prompt、工具权限和独立消息历史。确定性的 `MultiAgentCoordinator` 只负责传递结构化结果和控制流程，不参与模型推理，因此不算第四个 Agent。

## 3. 模块职责

- `config.py`：从环境变量读取 API 地址、模型、工作区和运行限制，不保存凭据。
- `llm.py`：构造 HTTP 请求并调用 OpenAI 兼容接口，处理 HTTP 和网络错误。
- `agent.py`：维护消息历史，校验模型响应，解析工具调用并控制循环终止。
- `multi_agent.py`：定义 Planner、Executor、Reviewer 的权限和提示词，并协调计划、执行、审查及一次返工。
- `tools.py`：定义工具 schema，在本地执行工具，并集中完成路径限制、权限确认、命令检查和审计日志。
- `cli.py`：解析命令行参数，组装上述组件，并把可理解的错误返回给用户。

## 4. 消息历史为什么能形成 Agent

第一次请求包含 system 消息和用户任务。模型要求调用工具时，程序先保存模型的 assistant 消息，再执行工具，并追加一条带相同 `tool_call_id` 的 tool 消息。下一次请求会包含这些历史，因此模型能看到真实执行结果并继续判断，而不是假设文件已经写入或测试已经通过。

本项目最多执行 20 步，所以选择保留完整历史。这个设计简单、可检查，适合本项目规模；更长任务可以增加摘要或滑动窗口，避免超过模型上下文限制。

多 Agent 模式不共享各角色的完整内部历史。协调器只传递：

```text
Planner -> plan
Executor -> execution summary
Reviewer -> approved / issues / summary
```

返工时创建新的 Executor 历史，第二次审查也创建新的 Reviewer 历史。这既控制上下文长度，也避免审查者被执行过程中的表述影响。

## 5. 三种终止条件

1. 模型没有返回工具调用：把普通文本作为最终答案。
2. 模型调用 `finish_task`：读取结构化总结并立即结束。
3. 达到最大步数：停止提供工具，只请求一次完成情况总结，防止无限循环和费用失控。

`finish_task` 使“任务完成”成为显式协议；最大步数则是独立于模型判断的兜底机制。

## 6. 安全设计与边界

安全控制分为三层：

1. 文件路径必须解析到工作区内部，阻止 `../` 越界。
2. 写文件、替换文件和执行命令默认需要用户确认，`--yes` 仅用于明确授权的演示场景。
3. `run_command` 在执行前拦截删除、关机、Git 推送、目录跳转等危险模式。

多 Agent 还执行角色权限隔离：Planner 只能列目录、读文件和提交计划；Reviewer 只能列目录、读文件、运行测试和提交审查结果。允许工具既通过 schema 限制，也在执行前再次检查，因此模型即使虚构写文件调用也不会执行。Reviewer 必须至少成功调用一次 `run_command`，才能提交通过结论。

工具日志会截断大段内容，并对 key、token、secret、password 以及常见 `sk-...` 凭据格式脱敏。

命令检查是应用层防护，不是操作系统沙箱，不能保证识别所有命令变体。生产级方案还应使用容器、低权限用户、系统调用隔离和网络策略。本项目选择可读的本地规则，是为了在题目规模内展示执行控制，而不是宣称完全隔离。

## 7. 主要设计取舍

### 为什么使用原生 tool calling

模型返回的工具名和参数是结构化 JSON，比从自然语言中提取命令稳定；实际工具仍由本地代码定义和执行，满足核心逻辑自行实现的要求。

### 为什么不让 `run_command` 负责写文件

文件修改优先使用 `write_file` 和 `replace_in_file`，这样路径检查、确认提示和日志更清楚。命令工具主要用于运行测试和查看结果。

### 为什么需要测试替身而不直接调用真实模型

真实模型响应有随机性、依赖网络并产生费用。测试使用预设响应的 `FakeChatClient`，可以稳定验证“工具调用 -> 结果回填 -> 结束”的控制逻辑，同时不需要 API Key。

### 为什么选择三个 Agent

三个角色恰好覆盖计划、实现和验证，职责容易解释。两个角色缺少独立规划或审查；四个以上角色会增加 API 调用、等待时间和上下文传递复杂度，却不会明显提高这个小项目的质量。

### 为什么多 Agent 是可选模式

命令行通过 `--multi-agent` 启用三 Agent 协作，原单 Agent 模式继续保留。这样既能展示协作创新，也保留稳定、低成本的回退路径。Reviewer 拒绝后最多返工一次，避免模型循环和费用失控。

## 8. 面试时可用的简短说明

大模型本身不会直接操作文件。程序把工具定义发送给模型，模型只返回结构化调用请求。本地控制器校验响应，经过路径限制、命令策略和用户确认后执行工具，再把结果作为 tool 消息加入对话历史。模型根据真实结果继续决策，直到显式调用 `finish_task`。系统还用最大步数防止无限循环，并用脱敏日志记录执行过程。

三 Agent 模式中，Planner 先提交结构化计划，Executor 完成代码和测试，Reviewer 使用只读权限独立复测。审查不通过时，协调器把具体问题交给新的 Executor 返工一次，再启动新的 Reviewer。所有角色仍使用项目自行实现的同一套工具循环，没有使用 Agent 框架。

常见追问：

- 模型说测试通过就可信吗？不可信，只有 `run_command` 返回退出码 0 和测试输出后才能确认。
- 为什么不只看最终文本判断完成？文本有歧义，`finish_task` 是更明确的机器可解析协议。
- 如何防止越界写文件？所有文件路径都先解析为绝对路径，再验证目标是否位于工作区下。
- 安全策略能否绝对安全？不能，它是应用层防护；真正强隔离需要容器或操作系统沙箱。
- 模型响应坏了怎么办？程序验证关键字段并给出明确错误，而不是因下标或键缺失产生难懂异常。
